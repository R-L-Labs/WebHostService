---
phase: 02-test-infrastructure
plan: 03
type: execute
---

<objective>
Write tests for payments controller covering financial calculations.

Purpose: Ensure payment calculations are correct and protected by tests before any future modifications.
Output: Comprehensive payments controller tests covering calculation logic.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./02-03-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-test-infrastructure/02-01-SUMMARY.md
@.planning/phases/02-test-infrastructure/02-02-SUMMARY.md

**Key files to test:**
@server/src/controllers/payments.controller.js

**Dependencies:**
- Vitest configured (from Plan 1)
- Prisma mock helper (from Plan 2)

**Important context:**
The payments controller has financial calculations that currently use parseFloat().
Phase 5 will replace these with decimal.js. Tests should document current behavior
so the decimal.js migration can verify no regressions.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests for getClientPayments calculation logic</name>
  <files>server/src/controllers/payments.controller.test.js</files>
  <test-first>
    Tests for getClientPayments:
    - Should return 404 if client not found
    - Should return empty array if no payments
    - Should calculate totalPaid correctly (sum of PAID payments)
    - Should calculate totalPending correctly (sum of PENDING payments)
    - Should handle mixed payment statuses correctly
  </test-first>
  <action>
    Create payments.controller.test.js.

    Setup:
    - Import getClientPayments from payments.controller.js
    - Import mockPrisma from test mocks
    - Create mock req, res, next objects
    - req.params = { clientId: 'test-uuid' }

    Test client not found:
    - mockPrisma.client.findUnique.mockResolvedValue(null)
    - Assert res.status called with 404

    Test empty payments:
    - mockPrisma.client.findUnique.mockResolvedValue({ id: 'test-uuid' })
    - mockPrisma.payment.findMany.mockResolvedValue([])
    - Assert res.json includes totalPaid: 0, totalPending: 0

    Test calculation accuracy:
    - Mock payments: [
        { amount: '100.00', status: 'PAID' },
        { amount: '50.50', status: 'PAID' },
        { amount: '75.25', status: 'PENDING' }
      ]
    - Assert totalPaid === 150.50
    - Assert totalPending === 75.25

    Note: Prisma Decimal comes as string, controller uses parseFloat().
    Tests should use string amounts matching Prisma's Decimal output.
  </action>
  <verify>cd server && npm test -- payments.controller.test.js</verify>
  <done>getClientPayments calculation tests pass. Totals verified for various scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests for createPayment and updatePayment</name>
  <files>server/src/controllers/payments.controller.test.js</files>
  <test-first>
    Tests for createPayment:
    - Should return 400 if clientId missing
    - Should return 400 if amount missing
    - Should return 400 if paymentDate missing
    - Should return 404 if client not found
    - Should create payment with correct data

    Tests for updatePayment:
    - Should return 404 if payment not found
    - Should update only provided fields
    - Should handle amount conversion correctly
  </test-first>
  <action>
    Add to payments.controller.test.js.

    Test createPayment validation:
    - Test each required field missing → 400
    - Mock client not found → 404

    Test successful create:
    - mockPrisma.client.findUnique.mockResolvedValue({ id: 'uuid' })
    - mockPrisma.payment.create.mockResolvedValue({ id: 'payment-uuid', amount: '100.00' })
    - req.body = { clientId: 'uuid', amount: '100.00', paymentDate: '2024-01-01' }
    - Assert res.status called with 201

    Test updatePayment:
    - Mock payment not found → 404
    - Mock existing payment, update with partial data
    - Verify prisma.payment.update called with correct spread

    Amount handling:
    - req.body.amount = '99.99' → should pass parseFloat in controller
    - Verify the value reaches Prisma correctly
  </action>
  <verify>cd server && npm test -- payments.controller.test.js</verify>
  <done>Create and update payment tests pass. Validation and data flow verified.</done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md and ROADMAP.md for phase completion</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    Update STATE.md:
    - Change Phase to: 3 of 6 (Security Hardening)
    - Change Plan to: Not started
    - Change Status to: Ready to plan
    - Update Last activity: Phase 2 complete (3 plans)
    - Update Progress bar to: ██░░░░░░░░ 33%
    - Update Current focus to: Phase 3 — Security Hardening
    - Update velocity metrics with Phase 2 stats

    Update ROADMAP.md:
    - Mark Phase 2 checkbox as complete: [x]
    - Update Plans for Phase 2: 3
    - Update progress table: 3/3 | Complete | date
  </action>
  <verify>
    cat .planning/STATE.md | grep "Phase: 3"
    cat .planning/ROADMAP.md | grep "\\[x\\].*Test Infrastructure"
  </verify>
  <done>State files reflect Phase 2 completion. Ready for Phase 3.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd server && npm test` passes all tests
- [ ] Payments controller has tests for all calculation logic
- [ ] Tests document current parseFloat behavior (baseline for Phase 5 decimal.js migration)
- [ ] STATE.md and ROADMAP.md updated
</verification>

<success_criteria>
- All tests pass: jwt.utils, password.utils, auth.controller, payments.controller
- Critical financial calculations have test coverage
- Test infrastructure complete and documented
- Phase 2 marked complete, ready for Phase 3
</success_criteria>

<output>
After completion, create `.planning/phases/02-test-infrastructure/02-03-SUMMARY.md`:

# Phase 2 Plan 3: Payments Controller Tests Summary

**[Substantive one-liner]**

## Accomplishments
- Payments controller test coverage
- Financial calculation tests document current behavior
- Phase 2 complete

## Files Created/Modified
- server/src/controllers/payments.controller.test.js
- .planning/STATE.md
- .planning/ROADMAP.md

## Test Coverage Summary
| File | Functions Tested |
|------|------------------|
| jwt.utils.js | generateToken, verifyToken |
| password.utils.js | hashPassword, comparePassword |
| auth.controller.js | login |
| payments.controller.js | getClientPayments, createPayment, updatePayment |

## Next Step
Phase 2 complete, ready for Phase 3: Security Hardening
</output>
