---
phase: 03-security-hardening
plan: 02
type: execute
---

<objective>
Add client-side role-based access control to ProtectedRoute component.

Purpose: Prevent unauthorized users from accessing admin-only routes on the frontend (defense in depth - server already enforces roles).
Output: ProtectedRoute component with allowedRoles prop, unauthorized users redirected appropriately.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./03-02-SUMMARY.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan context:
@.planning/phases/03-security-hardening/03-01-SUMMARY.md

**Key files to modify:**
@client/src/components/common/ProtectedRoute.jsx
@client/src/store/authStore.js

**Current state:**
- ProtectedRoute only checks isAuthenticated and token
- authStore contains user object with role property
- Roles in system: ADMIN, SUPER_ADMIN

**Tech stack available:** React, Zustand, React Router
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add allowedRoles prop to ProtectedRoute component</name>
  <files>client/src/components/common/ProtectedRoute.jsx</files>
  <action>
    Update ProtectedRoute to accept optional allowedRoles prop:

    ```jsx
    export default function ProtectedRoute({ children, allowedRoles }) {
      const { isAuthenticated, token, user } = useAuthStore();

      // Check authentication first
      if (!isAuthenticated || !token) {
        return <Navigate to="/admin/login" replace />;
      }

      // If roles specified, check user has one of the allowed roles
      if (allowedRoles && allowedRoles.length > 0) {
        if (!user?.role || !allowedRoles.includes(user.role)) {
          // Redirect to dashboard with unauthorized message
          return <Navigate to="/admin" replace />;
        }
      }

      return children;
    }
    ```

    Key points:
    - allowedRoles is optional - if not provided, only checks authentication
    - Uses array.includes() to check if user role is in allowed list
    - Redirects unauthorized users to /admin (dashboard) not login
    - Handles edge case where user object might not have role
  </action>
  <verify>
    Manual verification:
    - Component renders children when authenticated and role matches
    - Component redirects to login when not authenticated
    - Component redirects to /admin when role not in allowedRoles
  </verify>
  <done>ProtectedRoute accepts allowedRoles prop and enforces role-based access.</done>
</task>

<task type="auto">
  <name>Task 2: Update project state for phase completion</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <action>
    1. Update STATE.md:
       - Phase: 4 of 6 (Database Optimization)
       - Plan: Not started
       - Status: Ready to plan
       - Last activity: [today] - Phase 3 complete (2 plans)
       - Progress: Update to reflect Phase 3 complete (~50%)
       - Add decision: "Client-side role checks are defense in depth (server enforces)"

    2. Update ROADMAP.md:
       - Mark Phase 3 checkbox complete: [x]
       - Update progress table: Phase 3 | 2/2 | Complete | [today]
  </action>
  <verify>
    STATE.md shows Phase 4 ready
    ROADMAP.md shows Phase 3 complete with 2/2 plans
  </verify>
  <done>Project state updated. Phase 3 complete, Phase 4 ready for planning.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ProtectedRoute.jsx has allowedRoles prop
- [ ] Role checking logic implemented correctly
- [ ] STATE.md updated for Phase 4
- [ ] ROADMAP.md Phase 3 marked complete
</verification>

<success_criteria>
- ProtectedRoute component supports role-based access
- Backward compatible (existing routes without allowedRoles still work)
- Phase 3 complete and documented
</success_criteria>

<output>
After completion, create `.planning/phases/03-security-hardening/03-02-SUMMARY.md`
</output>
