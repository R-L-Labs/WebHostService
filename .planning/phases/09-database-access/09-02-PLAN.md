---
phase: 09-database-access
plan: 02
type: execute
---

<objective>
Create Supabase query utilities for payments and users.

Purpose: Complete the query layer for all entities, replacing remaining Express API calls.
Output: Query modules for payments and users, phase 9 complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/09-database-access/09-01-SUMMARY.md

**Key files:**
@client/src/lib/supabase.js
@client/src/utils/api.js
@client/src/lib/queries/clients.js (created in 09-01)

**Current API patterns (from api.js):**
- paymentsAPI: getByClient, getOne, create, update, delete
- usersAPI: getAll, getOne, resetPassword

**Database considerations:**
- payments table has client_id foreign key
- payments.amount is Decimal - handle as string or number carefully
- users table linked to auth.users via auth_id column
- Password reset now goes through Supabase Auth (not direct DB update)

**Established patterns from 09-01:**
- Soft delete filtering: .is('deleted_at', null)
- Return format: { data, error }
- Use .select().single() for insert/update returns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create payments query module</name>
  <files>client/src/lib/queries/payments.js</files>
  <action>
    Create query module for payments table:

    ```javascript
    import { supabase } from '../supabase'

    export async function getPaymentsByClient(clientId) {
      // Get all payments for a specific client
      // Order by payment_date desc (newest first)
      // Exclude deleted
      // Return: { payments, error }
    }

    export async function getPayment(id) {
      // Single payment by ID
      // Return: { payment, error }
    }

    export async function createPayment(data) {
      // Insert new payment
      // data includes: client_id, amount, payment_date, description, status
      // Return: { payment, error }
    }

    export async function updatePayment(id, data) {
      // Update existing payment
      // Return: { payment, error }
    }

    export async function deletePayment(id) {
      // Soft delete
      // Return: { error }
    }
    ```

    Note: amount field may be Decimal in DB - Supabase returns it as string, which is fine for display.
  </action>
  <verify>File created with all 5 functions</verify>
  <done>Payments query module ready</done>
</task>

<task type="auto">
  <name>Task 2: Create users query module</name>
  <files>client/src/lib/queries/users.js</files>
  <action>
    Create query module for users table:

    ```javascript
    import { supabase } from '../supabase'

    export async function getUsers() {
      // Get all users
      // Select: id, email, first_name, last_name, role, created_at
      // Exclude password_hash (security)
      // Exclude deleted
      // Return: { users, error }
    }

    export async function getUser(id) {
      // Single user by ID
      // Exclude password_hash
      // Return: { user, error }
    }

    // Note: resetPassword is now handled via Supabase Auth
    // Use supabase.auth.admin.updateUserById() or password reset flow
    // For now, just export a stub that returns an error explaining this
    export async function resetPassword(id, newPassword) {
      // This needs Supabase service role key (server-side only)
      // In client-side app, use password reset email flow instead
      return {
        error: {
          message: 'Password reset requires email flow. Use supabase.auth.resetPasswordForEmail()'
        }
      }
    }
    ```

    The resetPassword function is a stub - actual password changes go through Supabase Auth email flow (already implemented in auth.js).
  </action>
  <verify>File created with all 3 functions</verify>
  <done>Users query module ready</done>
</task>

<task type="auto">
  <name>Task 3: Create index file for query modules</name>
  <files>client/src/lib/queries/index.js</files>
  <action>
    Create barrel export for all query modules:

    ```javascript
    // Re-export all query functions for easy importing
    export * from './clients'
    export * from './inquiries'
    export * from './packages'
    export * from './payments'
    export * from './users'
    ```

    This allows:
    ```javascript
    import { getClients, getPaymentsByClient } from '@/lib/queries'
    ```
  </action>
  <verify>Index file created, build passes</verify>
  <done>Query modules have clean barrel export</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] client/src/lib/queries/payments.js exists with 5 functions
- [ ] client/src/lib/queries/users.js exists with 3 functions
- [ ] client/src/lib/queries/index.js exports all modules
- [ ] No import/syntax errors
- [ ] `npm run build` passes
</verification>

<success_criteria>
- All 5 entity query modules complete (clients, inquiries, packages, payments, users)
- Barrel export provides clean import path
- Phase 9 complete - ready for Phase 10 (Frontend Refactor)
</success_criteria>

<output>
After completion, create `.planning/phases/09-database-access/09-02-SUMMARY.md`

Include:
- Payments and users query modules created
- Index file for barrel exports
- Phase 9 complete
- Ready for Phase 10 (Frontend Refactor)

Also update ROADMAP.md to mark Phase 9 complete.
</output>
