---
phase: 05-code-quality
plan: 02
subsystem: error-handling, currency
tags: [error-handling, decimal.js, currency, defensive-coding]

# Dependency graph
requires:
  - phase: 05-code-quality
    plan: 01
    provides: Structured logging for error visibility
provides:
  - Safe JSON.parse with error handling
  - Accurate currency calculations with decimal.js
  - Phase 5 completion
affects: [packages, payments]

# Tech tracking
tech-stack:
  added: [decimal.js]
  patterns: [Defensive JSON parsing, Decimal arithmetic for currency]

key-files:
  created: []
  modified:
    - server/src/controllers/packages.controller.js
    - server/src/controllers/payments.controller.js
    - server/package.json

# Plan
discovery: Level 0 (Skip - established patterns)
estimated-tasks: 3
---

# Phase 5 Plan 2: Error Handling and Currency Fixes

**Add defensive JSON.parse handling and fix floating-point currency calculations**

## Goal

Improve code robustness with:
- Safe JSON parsing that won't crash on malformed data
- Accurate currency calculations using decimal.js (no floating-point errors)

## Pre-Execution Checklist

- [x] Identified JSON.parse calls: packages.controller.js lines 20, 55
- [x] Identified currency calculations: payments.controller.js lines 34, 38, 88, 132
- [x] decimal.js selected (industry standard for currency) - Decision in PROJECT.md

## Tasks

### Task 1: Add Safe JSON.parse to Packages Controller

**Goal:** Prevent crashes from malformed JSON in package features

**Commit Type:** `fix`

Steps:
1. Create helper function `safeParseJSON(str, fallback)` in packages.controller.js
2. Replace `JSON.parse(pkg.features)` on line 20 with `safeParseJSON(pkg.features, [])`
3. Replace `JSON.parse(pkg.features)` on line 55 with `safeParseJSON(pkg.features, [])`
4. Log warning when JSON parse fails (use logger from Plan 1)

**Verification:**
- [ ] Malformed JSON returns empty array instead of throwing
- [ ] Warning logged for parse failures
- [ ] Normal operation unchanged

### Task 2: Install decimal.js and Fix Payment Calculations

**Goal:** Eliminate floating-point errors in currency totals

**Commit Type:** `fix`

Steps:
1. Install decimal.js
2. Import Decimal in `server/src/controllers/payments.controller.js`
3. Replace parseFloat sums (lines 34, 38) with Decimal arithmetic:
   ```js
   const totalPaid = payments
     .filter((p) => p.status === 'PAID')
     .reduce((sum, p) => sum.plus(p.amount), new Decimal(0))
     .toNumber();
   ```
4. Keep parseFloat for storage (lines 88, 132) - Prisma Decimal handles conversion

**Verification:**
- [ ] Currency calculations are precise
- [ ] Existing payment tests pass
- [ ] API responses unchanged

### Task 3: Update State and Complete Phase 5

**Goal:** Document completion and update project tracking

**Commit Type:** `docs`

Steps:
1. Create 05-02-SUMMARY.md with accomplishments
2. Update STATE.md:
   - Current Position: Phase 6
   - Progress: 83%
   - Performance metrics
3. Update ROADMAP.md:
   - Mark Phase 5 complete
   - Update progress table

**Verification:**
- [ ] Summary documented
- [ ] STATE.md reflects Phase 6 ready
- [ ] ROADMAP.md shows Phase 5 complete

## Completion Checklist

- [ ] All 3 tasks committed
- [ ] JSON.parse has error handling
- [ ] Currency uses decimal.js
- [ ] All tests pass
- [ ] Phase 5 complete

## File Changes Summary

| File | Change |
|------|--------|
| server/package.json | Add decimal.js |
| server/src/controllers/packages.controller.js | Safe JSON.parse |
| server/src/controllers/payments.controller.js | Decimal arithmetic |
| .planning/phases/05-code-quality/05-02-SUMMARY.md | Plan summary |
| .planning/STATE.md | Advance to Phase 6 |
| .planning/ROADMAP.md | Mark Phase 5 complete |
